import { TimeSelectedDirective } from './time-selected.directive';

describe('TimeSelectedDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeSelectedDirective();
    expect(directive).toBeTruthy();
  });
});
